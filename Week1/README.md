# Week 1 - Java Design Patterns

This project contains simple Core Java examples for ten common design patterns. Each folder is independent and has its own `Main.java`.

## Run an example

Open a pattern folder and run:

```text
javac *.java
java Main
```

Compile each pattern folder separately because every example intentionally has its own `Main` class in the default package.
